# Deprecated local `diagnose`

This is the pre-modernization local version of `diagnose`. The active skill now uses Matt Pocock's version as the baseline, with local refinements carried forward where they add clear value.
