# x-dotfiles-script recipe

Use `/x-dotfiles-script` when creating or migrating a shell tool into the personal `x_` dotfiles convention.

The skill covers naming, placement under `stow/scripts`, `_x_manifest` registration, executable permissions, and docs updates for the dotfiles MkDocs site.
