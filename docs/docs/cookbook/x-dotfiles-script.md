# x-dotfiles-script cookbook

Ask for a new `x_` command by describing the behavior, expected inputs, and examples. The skill should keep the command small, register it in `_x_manifest`, and update the relevant dotfiles docs.
